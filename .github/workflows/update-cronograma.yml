name: Atualizar Cronograma no Portal

on:
  repository_dispatch:
    types: [cronograma-atualizado]

  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-cronograma:
    runs-on: ubuntu-latest

    steps:
      - name: 🧬 Checkout do portal-relevo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.RELEVO_GH_PAT }}

      - name: 🧬 Checkout do cronogramas-relevo
        uses: actions/checkout@v4
        with:
          repository: RelevoAmbiental/cronogramas-relevo
          token: ${{ secrets.RELEVO_GH_PAT }}
          path: cronogramas-src

      - name: 🔧 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: |
            cronogramas-src/package-lock.json
            cronogramas-src/package.json

      - name: 📦 Instalar dependências do cronograma
        working-directory: cronogramas-src
        run: npm ci

      - name: 🏗️ Build do cronograma (Vite)
        working-directory: cronogramas-src
        run: npm run build

      - name: 🧹 Limpar pasta cronograma atual
        run: |
          rm -rf cronograma
          mkdir cronograma

      - name: 📁 Copiar build completo para portal/cronograma
        run: |
          cp -R cronogramas-src/dist/* cronograma/

      - name: 💾 Commit e Push das alterações
        run: |
          git status
          if git diff --quiet; then
            echo "✅ Sem mudanças para commitar."
            exit 0
          fi

          git config --local user.email "bot@relevo.eco.br"
          git config --local user.name "RelevoBot"

          git add cronograma/
          git commit -m "Atualização automática do Cronograma (build completo)"
          git push

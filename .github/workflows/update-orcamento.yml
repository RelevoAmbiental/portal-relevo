name: Atualizar Orçamento no Portal

on:
  workflow_dispatch:
    inputs:
      run_id:
        description: 'Run ID do workflow do orçamento'
        required: true
        type: string

jobs:
  update-orcamento:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout código do portal
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: 📥 Download artifact do orçamento
      uses: actions/download-artifact@v4
      with:
        name: orcamento-build
        run-id: ${{ inputs.run_id }}
        repository: 'RelevoAmbiental/orcamento-relevo'
        
    - name: 🔓 Descompactar artifact
      run: |
        unzip -o orcamento-build.zip -d temp-unzipped/
        
    - name: 📁 Copiar para pasta do portal
      run: |
        rm -rf orcamento/
        mkdir -p orcamento
        cp -r temp-unzipped/* orcamento/
        echo "✅ Arquivos copiados para pasta orcamento/"
        ls -la orcamento/
        
    - name: 💾 Commit e Push
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add orcamento/
        git commit -m "🔄 Atualização automática do Sistema de Orçamentos"
        git push
